name: Enviroment
on: [push, workflow_dispatch]
env:
  NODE-ENV: production
  GH-SECRET: 52
jobs:
  build:
    runs-on: ubuntu-latest
    # needs: lint
    steps:
      - name: Print env Build
        run: |
          echo ${{ env.NODE-ENV }}
          echo ${{ env.GH-SECRET }}
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print env Deploy
        run: |
          echo ${{ env.NODE-ENV }}
          echo ${{ env.GH-SECRET }}
