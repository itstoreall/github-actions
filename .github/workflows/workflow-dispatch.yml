name: First Workflow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hi man!
      - name: Print few lines
        run: |
          echo First line
          echo Second line
